// Compile-only assertions that the adapter's helper types line up with `ai`'s
// (e.g. `prepareStep` is assignable to `PrepareStepFunction`). Populated with
// the factory surface; this stub keeps `tsc -p tsconfig.type-tests.json` honest
// from the first commit.
export {};
