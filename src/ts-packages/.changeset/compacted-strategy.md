---
"agentified": minor
---

Replace `summary` and `recent+summary` strategies with unified `compacted` strategy. Add `pruneThreshold` option to prune long tool results before summarization. Add `compactionStrategy` callback for client-side custom summarization.
