# Progress for Issue #17

Started: Mon Mar  9 23:53:04 CET 2026

## Completed

### Task 1: Crate split ✅
- Cargo workspace: agentified-lib (library) + agentified-server (binary)
- AgentifiedCore struct in lib with register_tools, list_tools, discover, capture_turn
- CoreError enum for domain errors (EmbeddingFailed, NotFound)
- Server is thin axum shell: handlers delegate to AgentifiedCore, map_error for HTTP status
- FakeEmbedding/FailingEmbedding gated behind `test-utils` feature
- All 46 tests pass (24 lib + 22 server)

### Task 2: Instances table + CRUD ✅
- instances table (instance_id, dataset_id, created_at, last_heartbeat) w/ indexes
- Storage trait: save_instance, get_instance, delete_instance, update_heartbeat
- AgentifiedCore: create_instance, heartbeat_instance, delete_instance
- POST /api/v1/instances → 201 + { instance_id }
- POST /api/v1/instances/{id}/heartbeat → 204 / 404
- DELETE /api/v1/instances/{id} → 204 / 404
- All 51 tests pass (24 lib + 27 server)

### Task 3: Instance-scoped tools ✅
- instance_id column added to tools table (composite PK: instance_id + name)
- Storage trait: save_tools(instance_id, ...), load_tools_for_instance(instance_id), delete_tools_for_instance(instance_id)
- AgentifiedCore: tools HashMap now HashMap<String, HashMap<String, StoredTool>> (instance → name → tool)
- Routes migrated: POST/GET /api/v1/instances/{id}/tools, POST /api/v1/instances/{id}/discover
- Instance existence verified on register/list/discover (404 if missing)
- Cascade delete: delete_instance removes tools from DB + in-memory
- Instance isolation: TC-R02, TC-R03, TC-R12 all covered
- All 55 tests pass (24 lib + 31 server)

### Task 4: Instance GC ✅
- Storage: get_expired_instances(cutoff) returns instance_ids where last_heartbeat < cutoff
- AgentifiedCore: gc_instances() finds expired instances (heartbeat > 5min), cascade-deletes tools from DB + in-memory
- Server: tokio::spawn 60s interval background task calling gc_instances
- Messages NOT deleted (belong to sessions, not instances)
- All 57 tests pass (26 lib + 31 server)

### Task 5: Messages table + endpoints ✅
- messages table (id, dataset_id, namespace_id, session_id, role, content, tool_call_id, tool_calls, created_at, seq)
- Storage trait: append_messages (BEGIN IMMEDIATE, MAX(seq)+1 per session), get_messages (3 pagination modes)
- CoreError::BadRequest variant for invalid params
- POST /api/v1/messages → appended + first_seq + last_seq
- GET /api/v1/messages → messages + has_more + max_seq
- Three pagination modes: default (last N asc), after_seq (forward), around_seq (centered window)
- limit=0 → no messages, returns max_seq; both params → 400
- Cross-session isolation: independent seq per (dataset, namespace, session)
- All 65 tests pass (26 lib + 39 server)

### Task 6: Context endpoint ✅
- POST /api/v1/context endpoint with body: { dataset, namespace, session, messages: { strategy, max_tokens } }
- AgentifiedCore::get_context() with full + recent strategies
- Token counting: char/4 heuristic
- recent: last N messages fitting budget (newest first, reversed to asc)
- full: all messages from oldest until budget exceeded
- summary/recent+summary → 422 "Summary strategies available in a future version"
- Defaults: strategy=recent, max_tokens=4000
- Response: messages, strategy_used, total_messages, included_messages, recalled (empty stubs), token_estimate, conversation_messages, fallback
- CoreError::UnsupportedStrategy variant → 422 HTTP status
- All 74 tests pass (31 lib + 43 server)

### Task 7: Instance lifecycle + instance-scoped signatures (SDK) ✅
- createInstance(dataset) → POST /api/v1/instances → { instanceId }
- heartbeatInstance(instanceId) → POST /api/v1/instances/{id}/heartbeat
- deleteInstance(instanceId) → DELETE /api/v1/instances/{id}
- register(instanceId) → POST /api/v1/instances/{id}/tools
- discover(instanceId, query, limit?, exclude?, turnId?) → public, POST /api/v1/instances/{id}/discover
- prefetch(instanceId, options) → delegates to instance-scoped discover
- captureTurn(instanceId, namespace, session, options) → POST /api/v1/instances/{id}/turns
- asDiscoverTool(instanceId) → uses instance-scoped discover
- All 19 SDK tests pass + 1 tool test = 20 total

### Task 8: Message persistence methods (SDK) ✅
- appendMessages(dataset, namespace, session, messages) → POST /api/v1/messages → { appended, firstSeq, lastSeq }
- getMessages(dataset, namespace, session, opts?) → GET /api/v1/messages?... → { messages, hasMore, maxSeq }
- getContext(dataset, namespace, session, opts?) → POST /api/v1/context → { messages, strategyUsed, totalMessages, ... }
- New types: StoredMessage, AppendMessagesResponse, GetMessagesOpts, GetMessagesResponse, ContextOpts, ContextResponse
- All types exported from index.ts
- snake_case server responses mapped to camelCase SDK types
- All 25 tests pass (24 ApiClient + 1 tool)

### Task 9: Agentified class + connect(url) + disconnect() ✅
- Agentified class: constructor, connect(serverUrl?), dataset(name), disconnect()
- connect(url): health check GET /health with 5s AbortSignal timeout, creates ApiClient
- connect() without URL: throws "Local spawn not yet implemented" (task 10)
- disconnect(): clears heartbeat intervals, deletes tracked instances (best-effort), kills spawned process, idempotent
- DatasetRef class: holds agentified ref + datasetName
- Internal state: sdk, connected flag, spawnedProcess, activeInstances Set, heartbeatIntervals Map
- Exported from index.ts
- All 58 tests pass (11 agentified + 31 adapter + 11 schema + 5 stream-sse)
